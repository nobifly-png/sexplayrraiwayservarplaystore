/** Default pagination for list endpoints (query validation + services). */
const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 50;
const MAX_LIMIT = 100;
const WALLET_TX_DEFAULT_LIMIT = 50;
const WALLET_TX_MAX_LIMIT = 100;

module.exports = {
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT,
  WALLET_TX_DEFAULT_LIMIT,
  WALLET_TX_MAX_LIMIT
};
